"""To copy from the local folder to vm compute engine.To copy folder we need to zip it."""
sudo gcloud compute scp localdirectory_path/parameters.zip  test-fluent-cloud:/home/nikitha --zone us-central1-b

"""Cloned"""
wget github_repo_link
